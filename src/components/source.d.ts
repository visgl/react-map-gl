
import {ReactElement} from 'react';

type SourceProps = {
  id?: string,
  type: string,
  children?: any,
  data?: any,
  coordinates?: any,
  url?: any,
  tiles?: any
};

export default function Source(props: SourceProps): ReactElement;
